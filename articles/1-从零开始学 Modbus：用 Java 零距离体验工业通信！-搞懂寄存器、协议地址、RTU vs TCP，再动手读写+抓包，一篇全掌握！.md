

# 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写，一篇全掌握！

你有没有想过：工厂里的温度传感器是怎么把数据传给电脑的？智能电表又是怎么被远程读取甚至修改参数的？
答案很可能就是——**Modbus 协议**。

Modbus 是工业自动化中最常用、最简单的通信协议之一。它免费、开放、稳定，被成千上万的设备支持。
我用 **Java + j2mod** 写个简单demo，可亲手体验 Modbus 通信，并彻底搞懂那些让人困惑的概念。



## 一、Modbus 是什么？一句话说清

> **Modbus 就是设备之间“说话”的通用语言。**

想象你有一个温控器（只会测温和控制加热）和一台电脑（想看温度或设置目标值）。

它们不是同一个厂家生产的，但只要都支持 Modbus，就能互相通信：

- 电脑问：“现在温度多少？” → 温控器答：“25.6℃”
- 电脑说：“把目标温度设为 30℃” → 温控器确认：“收到！”

**这种“问-答”甚至“命令-执行”过程，就是 Modbus 的核心。**

### 🖼️ Modbus 主从通信流程图

![Modbus 主从通信流程图](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119214226858.png)

> **图注**：Modbus 是典型的“主从模式”。**主站**（Master）发起请求，**从站**（Slave）响应。所有通信由主站驱动。





## 二、关键概念：寄存器、线圈、地址……

在 Modbus 世界里，设备内部的数据不是随便放的，而是存在一些“小格子”里。

这些格子有不同类型，统称为 **数据模型**。最常见的四种是：

| 类型           | 英文名           | 特点                       | 常见用途                         |
| -------------- | ---------------- | -------------------------- | -------------------------------- |
| **线圈**       | Coil             | 只能存 **0 或 1**（开/关） | 控制继电器、电机启停             |
| **离散输入**   | Discrete Input   | 只读的 **0/1**             | 读取按钮、限位开关状态           |
| **输入寄存器** | Input Register   | 只读的 **16位整数**        | 读取传感器原始值（如温度、压力） |
| **保持寄存器** | Holding Register | 可读可写的 **16位整数**    | 存设定值、配置参数               |

> ✅ **寄存器 = 设备内部的一个“数据格子”，每个占 2 字节（16 位）**。

### 那“寄存器地址”到底怎么算？

很多设备说明书会写：“温度值在 **40001** 寄存器”。
但你在程序里却要写 `address = 0` —— 这是怎么回事？

其实，**40001 是“用户习惯地址”**，而 **0 是“协议实际地址”**。

> 🔑 **记住这个公式：**
> **协议地址 = 用户地址 - 起始编号**
> 比如：40001 → 40001 - 40001 = **0**
> 比如：40010 → 40010 - 40001 = **9**

所以，当需要读“地址0的保持寄存器”，实际上就是在读设备文档里的 **40001**！



## 三、Modbus 有两种“说话方式”：RTU 和 TCP

Modbus 是“语言规则”，但它可以在不同“传输通道”上运行。最常见的是两种：

### 1. **Modbus RTU（串口通信）**

- 使用 **RS-485 / RS-232 串口线**；
- 数据以**二进制**发送，紧凑高效；
- 适用于短距离、多设备（比如一条线上挂10个传感器）；
- 需要设置波特率（如 9600）、校验位等；
- 报文末尾带 **CRC 校验**，防止数据出错。

### 2. **Modbus TCP（以太网通信）**

- 使用 **网线 + IP 网络**；
- 数据封装在 **TCP/IP 包**中；
- 不需要设置波特率，像上网一样；
- 报文开头多了 **6字节 MBAP 头**（用于标识事务、长度等）；
- 依靠 TCP 协议保证可靠性，**不需要 CRC**。

### 🖼️ RTU 与 TCP 报文结构对比

![RTU 与 TCP 报文结构对比](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119214932493.png)

> **图注**：核心内容（功能码+地址+数量）完全相同，只是“包装”不同。
>
> TCP 更适合现代网络环境。



## 四、动手实践

### 所需工具（全部免费，直接下载使用）

- **从站模拟器**：`diagslave`（https://www.modbusdriver.com/diagslave.html）
- **图形化客户端**：`QModMaster`（https://sourceforge.net/projects/qmodmaster/）
- **抓包工具**：`Wireshark`（https://www.wireshark.org/）

### 启动 Modbus 从站（`diagslave`）

使用非常简单，下载后解压到一个位置，进入到 `bin` 目录，执行：

```powershell
.\diagslave.exe -m tcp -p 502 -a 1
```

![image-20260119222306100](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119222306100.png)

### QModMaster 使用指南

QModMaster 是一个开源的 Modbus 主站（客户端）工具，支持 Windows/Linux/macOS，界面简洁，功能强大。

#### 步骤 1：下载并启动

- 访问：https://sourceforge.net/projects/qmodmaster/
- 下载对应系统的版本（Windows 用户选 `.exe`）
- 双击运行，无需安装

#### **步骤 2：连接到 diagslave（从站）**

![image-20260119221951079](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119221951079.png)

✅ 如果连接成功，窗口左下角会显示 绿色圆点。

下面我将通过两个典型场景，用 Java 与 Modbus 设备交互。

## 五、场景一：读取寄存器 —— 获取设备实时数据

> 前提是要先启动从站模拟器 diagslave

#### 应用背景

假如你有一台温控器，它通过 Modbus TCP 暴露当前温度值在 **保持寄存器 40001**（协议地址 0）。
你的 Java 程序需要每 5 秒读取一次温度，并记录到日志或数据库中。

> 💡 这是**最常见**的 Modbus 使用场景：**采集数据**。

我提前通过 QModMaster 提前将温控器将当前温度（放大10倍）存于 **40001（地址0）**：

![image-20260119225356773](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119225356773.png)

然后我们通过代码读取。

### Show you the code

```java
@Test
void test_read() {
    try {
        // 1. 创建 TCP 连接
        TCPMasterConnection conn = new TCPMasterConnection(InetAddress.getByName("127.0.0.1"));
        conn.setPort(502);
        conn.connect();

        // 2. 构造读请求：地址2，读1个寄存器
        ReadMultipleRegistersRequest req = new ReadMultipleRegistersRequest(1, 1);
        req.setUnitID(1); // 从站ID=1

        // 3. 发送请求
        ModbusTCPTransaction trans = new ModbusTCPTransaction(conn);
        trans.setRequest(req);
        trans.execute();

        // 4. 解析响应
        ReadMultipleRegistersResponse res = (ReadMultipleRegistersResponse) trans.getResponse();
        int rawValue = res.getRegisterValue(0); // 假设返回 256

        // 5. 转换为实际温度（设备手册说明：值×0.1）
        double temperature = rawValue / 10.0;
        System.out.println(" 当前温度: " + temperature + " ℃");

        conn.close();
    } catch (Exception e) {
        System.err.println(" 读取失败: " + e.getMessage());
        log.error(e.getMessage());
    }
}
```

> 💡 **说明**：ReadMultipleRegistersRequest 这个请求就是读保持寄存器地址的请求，`count=1` 就是读单个寄存器，对应**功能码 03**。

运行结果：

![image-20260119225608389](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119225608389.png)

**关键点**：

- 读寄存器用于**监控**；
- 数据可能是整型，需按设备手册做缩放（如 ÷10、÷100）；
- 功能码 **03（Read Holding Registers）** 是最常用的读操作。

## 六、场景二：写入寄存器 —— 远程控制设备

#### **应用背景**

同一台温控器，允许你通过 **保持寄存器 40002**（协议地址 1）设置“目标温度”。
我将用 Java 程序将目标温度设为 **30.0℃**。

> 💡 这是**控制类**场景：**下发指令或参数**。

### Show you the code

```java
@Test
void test_write() {
    try {
        // 1. 创建连接
        TCPMasterConnection conn = new TCPMasterConnection(InetAddress.getByName("127.0.0.1"));
        conn.setPort(502);
        conn.connect();

        // 写入：地址1（即40002），值=300
        WriteSingleRegisterRequest req = new WriteSingleRegisterRequest(
                1,                    // 寄存器地址
                new SimpleRegister(300) // 值
        );
        req.setUnitID(1);

        ModbusTCPTransaction trans = new ModbusTCPTransaction(conn);
        trans.setRequest(req);
        trans.execute();

        WriteSingleRegisterResponse res = (WriteSingleRegisterResponse) trans.getResponse();
        System.out.println("res: " + res);
        System.out.println("✅ 目标温度已设为 30.0 ℃");

        conn.close();
    } catch (Exception e) {
        log.error("❌ 写入失败: {}", e.getMessage(), e);
    }
}
```

运行结果：

![image-20260119224232271](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119224232271.png)

通过图形化界面也能看到该寄存器的值已经改为了 300：

![image-20260119224049212](E:\文章\🌟 从零开始学 Modbus：用 Java 零距离体验工业通信！-搞懂寄存器、协议地址、RTU vs TCP，再动手读写+抓包，一篇全掌握！.assets\image-20260119224049212.png)





## 七、小结

- ✅ **Modbus 是主从式协议**，主站发起，从站响应；
- ✅ **四类数据空间**：线圈、离散输入、输入寄存器、保持寄存器；
- ✅ **地址规则**：40001 → 程序地址 0；
- ✅ **RTU（串口） vs TCP（网线）**：核心一致，封装不同；
- ✅ **读 = 监控（功能码 03）**，**写 = 控制（功能码 06）**；

